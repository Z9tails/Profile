:root {
    --royalblue:#0a16f3;


}
* {
    box-sizing:border-box;
    margin: 0px;
    padding: 0px;


}
body {
    color: var(--royalblue);
    font-family: Georgia, 'Times New Roman', Times, serif;
}

main {
    display:flex;
    flex-wrap: wrap;
    justify-content: space-between;
    background-image: linear-gradient(to bottom right, yellow,red);
    background-color: skyblue;
    flex-wrap:wrap

}
/* Header and Nav styles start */
header {
    padding: 1px 1px;
    background-color: whitesmoke;
    display: flex;
    box-sizing: border-box;
    justify-content: space-between;
    flex-wrap: wrap;
    position: sticky;
    position: -webkit-sticky;
    top: 0;
    background-image: url("../images/landingstrip.jpg");
    background-repeat: no-repeat;
    background-size: cover;
    background-attachment: fixed;
    background-position: center;
}
header h1 {
    font-weight: bold;
    margin: 0;
    font-size: 36px;
    color: whitesmoke;
    text-shadow: 3 3 10px rgba(0, 0, 0, 0.5);
    z-index: 9999;
}


header nav {
    margin: 7px 0;
}

header nav ul {
    display: flex;
    flex-wrap: wrap;
    justify-content: right;
    align-items:flex-end;
    list-style: none;
}

header nav ul li a {
    padding: 10px 15px;
    font-weight: lighter;
    font-size: 1vw;
    text-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
}

header nav ul li a:hover {
    background: var(--royalblue);
    color:burlywood;
    border-radius: 1vh;
    text-shadow:whitesmoke;
}

/* Hero Style Start */
.hero {
    background-size: cover;
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    align-items: flex-start;
    background-attachment: fixed;
    background-position: 80%;
}

.hero-cta {
    width: 50%;
    text-align: left;
    margin: 2%;
    color: #fff;
    font-size: 18px;
    line-height: 1.2;
}

.face-shot {
        width:auto;
        height:auto;
}
hero h2 {
    align-self: center;
}
/* End Hero Styles */

/* Begin Footer Styles */
.site-footer {
    background: brown;
    padding: 1px 1px;
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    width: 100%;
}

#footer3 {
    color: whitesmoke;
    font-size: 20px;
    margin: 0;
}

#footer div {
    line-height: 1.5;
    text-align: left;
}

#footerp {
    color:rgb(241, 41, 41);
}
/* End footer styles */




.flex-card {
    display:flex;
    flex-direction: row;
    flex-wrap: wrap;
    height: auto;
    width:auto;
    overflow: contain;
    padding: 10px 0px 0px;
    justify-content:space-around;
    box-shadow: #0a16f3;

}


.flex-card h2 {
    padding: 20px;
    background: #13293d;
    color: #fff;
}


.projectcards {
    display:flex;
    flex-wrap: wrap;
    align-items: flex-end;
    flex: 3 0 0;
    padding: 30px;
    text-align: center;
    border-style: solid;
    border-width: 1px;

}
.pjcard1 img {
    width:40vh;

}

.promotion img {
    width:40vh;
}

.promotion {
    display: flex;
    flex-wrap: wrap;
    flex: 1 0 0;
    padding: 10px;
    text-align: center;
    border-style: solid;
    border-width: 1px;

}
@media screen and (max-width: 800px) {

    .left,
    .main,
    .right {
        width: 100%;
        /* The width is 100%, when the viewport is 800px or smaller */
    }
}


@media screen and (max-width: 500px) {

    .body,
    .main,
    .flex-card, .projectcards {
        width: 100%;
        
    }
}